import MainPage from "./pages/MainPage";

export default function Home() {

  const myarr:number[] = [2,4,6,8,10];
  // using the above array and store the value to new array and 
  // remove 4,6,8 
  // const [first,,,,last] = myarr
  // const newarr = [first, last];
  // console.log(newarr)

function cal(num: number) : number{
  return num;
}
let employee : {id: number , name: string} =  {
  id: 1,
  name: "thanak"
}

const myarr1:number[] = [1,3,5,7,9];

  return (
    <div className="sadasd">
      <MainPage/>
    </div>
  );
}
